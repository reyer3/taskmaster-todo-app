name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      - name: Install Dependencies
        run: |
          echo "This is a placeholder for lint check"
          # Cuando tengas código real, descomenta estas líneas
          # npm ci
          # npm run lint
      - name: Check Code Style
        run: |
          # Placeholder para demostración
          echo "Lint check passed"
          exit 0

  build:
    name: Build Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      - name: Install Dependencies
        run: |
          echo "This is a placeholder for build check"
          # Cuando tengas código real, descomenta estas líneas
          # npm ci
      - name: Build Project
        run: |
          # Placeholder para demostración
          echo "Build check passed"
          exit 0
